# Configurations UI / HUD / menus

> Procédure personnelle du projet **SKYFORGE**. Aucun fichier de mod ou fichier soumis à redistribution restreinte n’est inclus dans ce dépôt.

Ce fichier documentera les réglages liés à l’interface, au HUD, aux menus, aux widgets, aux notifications, à la carte et aux fichiers de configuration associés.

---

## Fichiers / mods concernés

- `SkyHUD.txt`
- `TrueHUD`
- `STB Widgets`
- `STB Active Effects`
- `Oxygen Meter 2`
- `Menu Maid 2`
- `Notification Filter`
- `A Matter of Time`
- `Compass Navigation Overhaul`
- `Better Third Person Selection`
- `Wheeler`
- `Dynamic Activation Key`
- `RaceMenu`
- fichiers de traduction / interface liés à l’UI

---

## Configurations déjà générées

### Menu Maid 2

**Fichiers générés :**

- `SKSE/Plugins/MenuMaid2.ini`
- `SKSE/Plugins/MenuMaid2_ConfigEditorIds.ini`

**Mod MO2 dédié :**

`Menu Maid 2 - Generated INI`

**Statut :**  
Fichiers déplacés depuis `Overwrite` vers le mod dédié. `Overwrite` vidé.

---

## Configurations différées déjà identifiées

- Configuration finale `SkyHUD` / `Vel’dun UI`.
- Patches Vel’dun UI selon les mods parents réellement installés.
- `Notification Filter` : configuration INI à faire plus tard selon les notifications générées par les futurs modules.
- `$Undress slider` de `RaceMenu Undress` : correction / traduction / acceptation à vérifier plus tard.
- `Photo Mode` : différé.
- `HideUI` : différé.
- `Better AltTab` : à installer plus tard avec test individuel ALT+TAB.

---

## Règle spécifique

Les réglages UI/HUD doivent être validés ingame, idéalement dans la salle Skyrim Unbound, afin de vérifier les chevauchements visuels, la lisibilité, les widgets actifs et les éventuels conflits de menus.

---

## Statut actuel

Le module `03 - UI HUD MENUS` est provisoirement clos après l’étape 178.

Les configurations fines restent différées jusqu’aux tests ingame plus complets.